(function() {
    'use strict';

    const CONFIG = {
        minWidth: 100,
        minHeight: 140,
        watchBuffer: 0.5,
        scrollDelay: 1000,
        photoDuration: 3,
        maxLoadWait:5000
    };

    let isRunning = false;
    let playDirection ='down';
    
})